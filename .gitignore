# Fetched YDB documentation. Rebuilt by ./download.py.
# Heavy (~100 MB per version) and not part of the published site.
cache/

# The contents of docs/ are the GH Pages site root (see README → GitHub Pages).
# That folder IS COMMITTED; commit it alongside code changes or after ./analyze.py.

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Claude Code: local session settings
.claude/settings.local.json
